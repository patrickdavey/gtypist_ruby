require 'spec_helper'

describe GtypistRuby do
  it 'has a version number' do
    expect(GtypistRuby::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
